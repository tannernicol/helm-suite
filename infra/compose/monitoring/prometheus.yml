global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "prometheus"
    static_configs:
      - targets: ["localhost:9090"]

  - job_name: "node-exporter"
    static_configs:
      - targets: ["node-exporter:9100"]

  # Uncomment if nvidia-exporter is enabled
  # - job_name: "nvidia-gpu"
  #   static_configs:
  #     - targets: ["nvidia-exporter:9445"]

  # Add more targets as needed:
  # - job_name: "caddy"
  #   static_configs:
  #     - targets: ["host.docker.internal:2019"]
